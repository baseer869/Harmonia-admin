/** Static, non-secret application metadata. */
export const siteConfig = {
  name: 'Harmonia Admin',
  description:
    'Multi-tenant SaaS reservation & concierge platform — admin CRM.',
  version: '0.1.0',
} as const;

/**
 * Users · API layer (module public contract)
 * The ONLY surface routes/hooks may call. Delegates to the service layer.
 * This is the seam for future extraction into a standalone backend.
 */
export {};

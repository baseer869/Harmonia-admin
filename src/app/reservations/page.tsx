import { PageHeader } from '@/components/layouts';

export default function ReservationsPage() {
  return (
    <PageHeader title="Reservations" description="Bookings and reservation lifecycle." />
  );
}
